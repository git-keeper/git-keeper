[server]
# hostname of the grading server
host = 172.18.0.42
# username and group of the grader user on the server
username = keeper
group = keeper

[email]
from_name = git-keeper
from_address = git-keeper@moravian.edu
#smtp_server = smtp.gmail.com
#smtp_port = 587
#email_username =
#email_password =
